apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: default
spec:
  containers:
  - name: test-container
    image: test-image
---
name: valid container name using apply configuration
applyConfiguration:
  spec:
    containers:
    - name: "valid-container-name"
---
name: valid container name using json patch
jsonPatch:
- { "op": "replace", "path": "/spec/containers/0/name", "value": "valid-container-name-2" }
---
name: valid container name using replace
replace:
  "spec.containers[0].name": "valid-container-name-3" 